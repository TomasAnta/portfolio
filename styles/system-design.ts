import { DefaultTheme } from "styled-components";

import {
    colors,
    spacings,
    layout,
    breakpoints,
    typography,
} from "./design-variables";

export const atomasTheme: DefaultTheme = {
    theme: "atomasTheme",
    colors,
    breakpoints,
    typography,
    spacings,
    layout,
};
